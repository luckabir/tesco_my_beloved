#ifndef MAIN_H
#define MAIN_H

class InputManager;

enum GameState {
    STATE_INTRO,
    STATE_MENU,
    STATE_PLAYING,
    STATE_SETTINGS,
    STATE_SCORE,
    STATE_PROFILE,
    STATE_SAVES,
    STATE_EXIT,
};

#endif